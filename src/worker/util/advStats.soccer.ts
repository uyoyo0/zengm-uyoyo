// Soccer's advanced values (xG, xA, and match rating) are produced by the
// event simulation, so there is no separate end-of-day aggregation pass.
const advStats = async () => {};

export default advStats;
